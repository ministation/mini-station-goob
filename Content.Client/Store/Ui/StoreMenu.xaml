<!--
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<DefaultWindow
    xmlns="https://spacestation14.io"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    Title="{Loc 'store-ui-default-title'}"
    MinSize="700 560"
    SetSize="760 600">
    <BoxContainer Orientation="Vertical" Margin="2">
        <!-- Header: balance + actions -->
        <PanelContainer Margin="4,4,4,2">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat
                    BackgroundColor="#12161FCC"
                    BorderColor="#C23B3B88"
                    BorderThickness="0,0,0,2"
                    ContentMarginLeftOverride="10"
                    ContentMarginRightOverride="8"
                    ContentMarginTopOverride="6"
                    ContentMarginBottomOverride="6" />
            </PanelContainer.PanelOverride>
            <BoxContainer Orientation="Horizontal">
                <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalAlignment="Center">
                    <Label Text="{Loc 'store-ui-balance-header'}" StyleClasses="LabelSubText" />
                    <RichTextLabel
                        Name="BalanceInfo"
                        HorizontalAlignment="Left"
                        Access="Public"
                        HorizontalExpand="True" />
                </BoxContainer>
                <Button
                    Name="WithdrawButton"
                    MinWidth="72"
                    Margin="4,0,0,0"
                    VerticalAlignment="Center"
                    Text="{Loc 'store-ui-default-withdraw-text'}" />
                <Button
                    Name="RefundButton"
                    MinWidth="72"
                    Margin="4,0,0,0"
                    VerticalAlignment="Center"
                    Text="{Loc 'store-ui-refund-text'}" />
            </BoxContainer>
        </PanelContainer>

        <LineEdit
            Name="SearchBar"
            Margin="4,2,4,4"
            PlaceHolder="{Loc 'store-ui-search-placeholder'}"
            HorizontalExpand="True" />

        <PanelContainer VerticalExpand="True" Margin="4,0,4,4">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#0B0E14" />
            </PanelContainer.PanelOverride>
            <BoxContainer Orientation="Horizontal" VerticalExpand="True">
                <!-- Category rail -->
                <PanelContainer MinWidth="148" MaxWidth="168" VerticalExpand="True" Margin="0,0,4,0">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat
                            BackgroundColor="#10141C"
                            BorderColor="#1E2533"
                            BorderThickness="0,0,1,0"
                            ContentMarginLeftOverride="4"
                            ContentMarginRightOverride="4"
                            ContentMarginTopOverride="4"
                            ContentMarginBottomOverride="4" />
                    </PanelContainer.PanelOverride>
                    <ScrollContainer HScrollEnabled="False" VerticalExpand="True" HorizontalExpand="True">
                        <BoxContainer Name="CategoryListContainer" Orientation="Vertical" HorizontalExpand="True">
                            <!-- Category buttons are added here by code -->
                        </BoxContainer>
                    </ScrollContainer>
                </PanelContainer>

                <!-- Listing cards -->
                <ScrollContainer
                    Name="StoreListingsScroll"
                    HScrollEnabled="False"
                    HorizontalExpand="True"
                    MinSize="100 256"
                    SizeFlagsStretchRatio="2"
                    VerticalExpand="True">
                    <BoxContainer
                        Name="StoreListingsContainer"
                        MinSize="100 256"
                        Orientation="Vertical"
                        Margin="2,2,6,2"
                        SizeFlagsStretchRatio="2"
                        VerticalExpand="True">
                        <!-- Listings are added here by code -->
                    </BoxContainer>
                </ScrollContainer>
            </BoxContainer>
        </PanelContainer>

        <!-- Footer -->
        <BoxContainer Orientation="Vertical" Name="TraitorFooter">
            <PanelContainer StyleClasses="LowDivider" />
            <BoxContainer Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Bottom">
                <Label Text="{Loc 'store-ui-traitor-flavor'}" StyleClasses="WindowFooterText" />
                <Label Text="{Loc 'store-ui-traitor-warning'}" StyleClasses="WindowFooterText"
                        HorizontalAlignment="Right" HorizontalExpand="True"  Margin="0 0 5 0" />
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</DefaultWindow>
